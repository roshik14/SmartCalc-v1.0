#ifndef CHECKS_H_
#define CHECKS_H_

#include "calc.h"

int is_invalid_expr(const char*);
int is_functon_key(unsigned int);
int check_for_x(const char*);
int is_x(char);
int is_unary(char);
int is_function_key(unsigned int);

#endif  // CHECKS_H_
